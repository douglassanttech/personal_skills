entra na base do castle click em prod, conecta no ec2 que fica vsp-castle-api la e me diz quais batchCodes estao com isDigitlapayment set to 1 . tem q entrar no ec2 pq precisa estar na

vpn os dados do banco estao dentro do ec2 na pasta ~/vsp-castle-api/.env
O banco e sql server

ssh p logar em prod:


ssh -i "/Users/douglas/Downloads/prod-vsp-castle-api-key.pem" ec2-user@ec2-18-206-127-185.